// Alias de /api/upload-file : certains composants (BundleEditorForm, SidebarLevel2)
// postent vers /api/upload. On réutilise exactement la même action (upload Shopify Files).
export { action } from "./api.upload-file";
